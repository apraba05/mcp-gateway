# Reproducible MCP Gateway demo

> Generated by `python3 scripts/demo.py`; source milestone only—no tag or release.

The demo builds the gateway and example binaries, starts a loopback MCP server, sends an authenticated `tools/call` through the gateway, and validates the audit event.

- HTTP status: `200`
- Audit result: `success`
- Client response: `{"id":2,"jsonrpc":"2.0","result":{"content":[{"text":"gateway-demo-ok","type":"text"}]}}`
- Credential exposure check: passed (fixture value absent from output and gateway logs)
